// eslint-disable-next-line
import { render } from "react-dom";

// Be sure to read the comments in the "public/index.html" file and the
// "App.js" file. Use Style Components to implement the CSS from the style
// block in the "public/index.html" file

// Uncomment the code below to render the component tree

// import { App } from "./App";

// render(<App />, document.getElementById("root"));
